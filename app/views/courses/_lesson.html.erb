<div>
  <h4>Lesson by <%= lesson.lesson_title %></h4>
  <p class="lesson"><%= lesson.body %></p>
  <p>Posted <%= distance_of_time_in_words(lesson.course.created_at, lesson.created_at) %> later</p>
</div>
